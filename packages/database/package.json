{
  "name": "@repo/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./client": {
      "types": "./dist/client.d.ts",
      "default": "./src/client.ts"
    },
    "./schema": {
      "types": "./dist/schema.d.ts",
      "default": "./src/schema.ts"
    },
    "./queries": {
      "types": "./dist/queries/index.d.ts",
      "default": "./src/queries/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsc",
    "dk:generate": "sst shell drizzle-kit generate",
    "migrate": "sst shell drizzle-kit migrate",
    "push": "sst shell drizzle-kit push",
    "studio": "sst shell drizzle-kit studio",
    "migrate:prod": "sst shell --stage production drizzle-kit migrate",
    "push:prod": "sst shell --stage production drizzle-kit push",
    "studio:prod": "sst shell --stage production drizzle-kit studio",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "generate-dbml": "tsx generate-dbml.ts"
  },
  "dependencies": {
    "@t3-oss/env-nextjs": "^0.13.8",
    "drizzle-orm": "^0.44.2",
    "drizzle-zod": "^0.8.2",
    "pg": "^8.16.3",
    "server-only": "^0.0.1",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^24.0.13",
    "@types/pg": "^8.15.4",
    "drizzle-dbml-generator": "^0.10.0",
    "drizzle-kit": "^0.31.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
